\documentclass[aspectratio=169]{beamer}
\usetheme{metropolis}

%% Suppress theorem redefinition from lib.tex (beamer defines it already)
\let\theorem\relax
\let\endtheorem\relax
\let\c@theorem\relax

%% stmaryrd for double brackets used by lib.tex macros
\usepackage{stmaryrd}

%% TICL paper macros
\input{lib}

%% Rust syntax highlighting for code listings
\definecolor{rustkey}{rgb}{0.5, 0.0, 0.33}
\definecolor{ruststr}{rgb}{0.0, 0.5, 0.0}
\definecolor{rustcmt}{rgb}{0.4, 0.4, 0.4}
\lstdefinelanguage{verus}{
  language=C,
  morekeywords={fn,let,mut,pub,struct,impl,self,loop,while,if,else,use,
    requires,ensures,invariant,decreases,spec,proof,Ghost,
    af,ag,au,eu,done,now,old},
  morecomment=[l]{//},
  morestring=[b]",
  sensitive=true,
}
\lstset{
  language=verus,
  basicstyle=\ttfamily\scriptsize,
  keywordstyle=\color{rustkey}\bfseries,
  commentstyle=\color{rustcmt}\itshape,
  stringstyle=\color{ruststr},
  showstringspaces=false,
  columns=flexible,
  breaklines=true,
  frame=none,
  aboveskip=0pt,
  belowskip=0pt,
}

%% Rust-specific macros
\newcommand{\rust}[1]{\ensuremath{\mathtt{#1}}}
\newcommand{\rlet}[3]{\ensuremath{\rust{let}~#1 = #2\rust{;}~#3}}
\newcommand{\rmut}[2]{\ensuremath{\rust{*}#1 = #2}}
\newcommand{\rawait}[1]{\ensuremath{#1\rust{.await}}}
\newcommand{\rasync}[1]{\ensuremath{\rust{async}~\{#1\}}}
\newcommand{\rwhile}[2]{\ensuremath{\rust{while}~#1~\{#2\}}}
\newcommand{\rloop}[1]{\ensuremath{\rust{loop}~\{#1\}}}
\newcommand{\rif}[3]{\ensuremath{\rust{if}~#1~\{#2\}~\rust{else}~\{#3\}}}
\newcommand{\rfn}[2]{\ensuremath{\rust{fn}~#1()~\{#2\}}}
\newcommand{\rcall}[2]{\ensuremath{#1(#2)}}
\newcommand{\PID}{\ensuremath{\mathtt{PID}}}

%% Vera-specific entailment (single-process)
\newcommand{\ventailsL}[3]{\ensuremath{[#1, \ #2 \Vdash_L ~#3 ]}}
\newcommand{\ventailsR}[3]{\ensuremath{[#1, \ #2 \Vdash_R ~#3 ]}}

%% wp notation (single-process)
\newcommand{\wpx}[2]{\ensuremath{\mathtt{wp}(#1,~#2)}}

%% WP notation (multi-process)
\newcommand{\Config}[3]{\ensuremath{(#1,~#2,~#3)}}
\newcommand{\WPx}[2]{\ensuremath{\mathtt{WP}(#1,~#2)}}

\title{Vera: Weakest Preconditions for TICL over Rust}
\subtitle{From Imp Structural Lemmas to Rust VCGen Rules}
\author{Lef Ioannidis}
\date{2026}

\begin{document}

\begin{frame}
\titlepage
\end{frame}

% ============================================================================
\section{Background: TICL over Imp}
% ============================================================================

\begin{frame}{TICL Structural Lemmas for Imp}
  \begin{block}{Core idea (from the TICL paper)}
    For a small imperative language \stimp with mutable state,
    the entailment $\sentailsL{S}{t}{m}{\varphi}$ is proved
    structurally via inference rules on the syntax of $t$.
  \end{block}

  \vspace{0.3cm}
  Key rules from \texttt{imp-lemmas.tex}:
  \begin{itemize}
    \item \textbf{Seq}$_S$AU$_L$: sequential composition decomposes into bind
    \item \textbf{If}$_S$AU$_L$: conditional branches both satisfy the obligation
    \item \textbf{While}$_S$AG: infinite loop with coinductive invariant $\R$
    \item \textbf{While}$_S$AU$_L$: terminating loop with ranking function $f$
  \end{itemize}

  \vspace{0.3cm}
  \alert{Goal:} Lift these to Rust syntax $\to$ define $\mathtt{wp}$ for Vera.
\end{frame}

\begin{frame}{Notation}
  \begin{align*}
    \sigma &\in \text{State (mutable references, heap)} \\
    \ventailsL{t}{\sigma}{\varphi} &\triangleq \text{``Rust term } t \text{ from state } \sigma \text{ satisfies } \varphi\text{''} \\
    \wpx{t}{\varphi} &\triangleq \text{weakest precondition on } \sigma \text{ s.t.\ } \ventailsL{t}{\sigma}{\varphi}
  \end{align*}

  \begin{block}{Temporal formulas}
    \begin{itemize}
      \item $\varphi ::= \now{P} \mid \AG{\varphi} \mid \AU{\varphi}{\varphi'} \mid \AN{\varphi}{\varphi'} \mid \varphi \land \varphi'$
      \item Sugar: $\AF{\varphi'} = \AU{\top}{\varphi'}$, \quad $\AX{\varphi'} = \AN{\top}{\varphi'}$
      \item $\now{P}$ where $P : \sigma \to \mathbb{P}$ --- state predicate on current state
      \item $\done{Q}$ where $Q : \tau \to \sigma \to \mathbb{P}$ --- predicate on return value and final state
    \end{itemize}
  \end{block}
\end{frame}

% ============================================================================
\section{Inference Rules (Single-Process)}
% ============================================================================

\begin{frame}{Assignment}
  \begin{block}{Rust: \quad $\rmut{x}{e}$}
  \end{block}
  {\footnotesize
  \begin{mathpar}
    \inferrule*[Right=AssignAU$_L$]
    {\ventailsR{e}{\sigma}{\AU{\varphi}{\done{\R}}} \\\\
     \forall~v, \sigma',~\R~v~\sigma' \to \ventailsL{\ISkip}{\sigma'[x \mapsto v]}{\AU{\varphi}{\varphi'}}}
    {\ventailsL{\rmut{x}{e}}{\sigma}{\AU{\varphi}{\varphi'}}}
  \end{mathpar}
  }
  Evaluating $e$ may take steps; $\varphi$ must hold along the path.
\end{frame}

\begin{frame}{Sequential Composition (let-binding)}
  \begin{block}{Rust: \quad $\rlet{x}{t}{u}$}
  \end{block}
  {\footnotesize
  \begin{mathpar}
    \inferrule*[Right=LetAU$_L$]
    {\ventailsR{t}{\sigma}{\AU{\varphi}{\done{\R}}} \\\\
     \forall~x,\sigma',~\R~x~\sigma' \to \ventailsL{u[x]}{\sigma'}{\AU{\varphi}{\varphi'}}}
    {\ventailsL{\rlet{x}{t}{u}}{\sigma}{\AU{\varphi}{\varphi'}}}
  \end{mathpar}
  }
  Direct lift of \textbf{BindAU$_L$} / \textbf{Seq}$_S$AU$_L$ from TICL.
\end{frame}

\begin{frame}{Conditional}
  \begin{block}{Rust: \quad $\rif{c}{t}{u}$}
  \end{block}
  {\footnotesize
  \begin{mathpar}
    \inferrule*[Right=IfAU$_L$]
    {\ventailsR{c}{\sigma}{\AU{\varphi}{\doneq{(b,\sigma')}{}}} \\\\
     {
       \begin{cases}
         \ventailsL{t}{\sigma'}{\AU{\varphi}{\varphi'}}, &\text{if } b \\
         \ventailsL{u}{\sigma'}{\AU{\varphi}{\varphi'}}, &\text{otherwise}
       \end{cases}
     }
    }
    {\ventailsL{\rif{c}{t}{u}}{\sigma}{\AU{\varphi}{\varphi'}}}
  \end{mathpar}
  }
  Evaluating $c$ may take multiple steps; $\varphi$ must hold along the path.
\end{frame}

\begin{frame}{While Loop (AU -- terminating)}
  \begin{block}{Rust: \quad $\rwhile{c}{t}$ \quad with \texttt{invariant} $\R$, \texttt{decreases} $f$}
  \end{block}
  {\footnotesize
  \begin{mathpar}
    \inferrule*[Right=WhileAU$_L$]
    {\R~\sigma \\\\
     {\begin{aligned}
      \forall~\sigma,~&\R~\sigma \to \ventailsR{c}{\sigma}{\AU{\varphi}{\doneq{(b,\sigma')}{}}} \\
      & \land \begin{cases}
        \ventailsL{t}{\sigma'}{\AU{\varphi}{\varphi'}} ~\lor \\
        \quad \ventailsR{t}{\sigma'}{\AU{\varphi}{\done{(\lambda~\sigma'' .~
            \R~\sigma'' \land f~\sigma'' < f~\sigma)}}}, &\text{if } b \\
        \varphi'(\sigma'), &\text{otherwise}
      \end{cases}
     \end{aligned}}}
    {\ventailsL{\rwhile{c}{t}}{\sigma}{\AU{\varphi}{\varphi'}}}
  \end{mathpar}
  }
\end{frame}

\begin{frame}{While Loop (AG -- infinite)}
  \begin{block}{Rust: \quad $\rwhile{c}{t}$ \quad with \texttt{invariant} $\R$ (no \texttt{decreases})}
  \end{block}
  {\footnotesize
  \begin{mathpar}
    \inferrule*[Right=WhileAG]
    {\R~\sigma \\\\
     {\begin{aligned}
      \forall~\sigma,~&\R~\sigma \to \\
      & \ventailsL{\rwhile{c}{t}}{\sigma}{\varphi} ~\land \\
      & \ventailsR{c}{\sigma}{\AU{\varphi}{\doneq{(\tm{true},\sigma)}{}}} ~\land \\
      & \ventailsR{t}{\sigma}{\varphi~\AU{}~\done{(\lambda~\sigma' \Rightarrow \R~\sigma')}}
     \end{aligned}}}
    {\ventailsL{\rwhile{c}{t}}{\sigma}{\AG{\varphi}}}
  \end{mathpar}
  }
  Coinductive hypothesis $\ventailsL{\rwhile{c}{t}}{\sigma}{\varphi}$; condition must always be true.
\end{frame}

\begin{frame}{Function Call}
  \begin{block}{Rust: \quad $\rcall{f}{\vec{e}}$}
  \end{block}
  {\footnotesize
  \begin{mathpar}
    \inferrule*[Right=CallAU$_L$]
    {\ventailsR{\rcall{f}{\vec{e}}}{\sigma}{\AU{\varphi}{\done{\R}}} \\\\
     \forall~x,\sigma',~\R~x~\sigma' \to \ventailsL{k~x}{\sigma'}{\AU{\varphi}{\varphi'}}}
    {\ventailsL{\rcall{f}{\vec{e}}}{\sigma}{\AU{\varphi}{\varphi'}}}
  \end{mathpar}
  }
  Instance of bind: call produces value $x$, continuation $k$ uses it.
\end{frame}

% ============================================================================
\section{Single-Process Weakest Preconditions}
% ============================================================================

\begin{frame}{wp: Assignment}
    \begin{align*}
    \wpx{\rmut{x}{e}}{\AU{\varphi}{\varphi'}} ~=~ \lambda~\sigma.~
    &~ \wpx{e}{\AU{\varphi}{\done{\R}}}~\sigma \\
    \land~& ~\forall~v, \sigma'.~ \R~v~\sigma' \to \wpx{\ISkip}{\AU{\varphi}{\varphi'}}~(\sigma'[x \mapsto v])
    \end{align*}
\end{frame}

\begin{frame}{wp: Let-binding (Sequential Composition)}
    \begin{align*}
    \wpx{\rlet{x}{t}{u}}{\AU{\varphi}{\varphi'}} ~=~ \lambda~\sigma.~
    &~ \wpx{t}{\AU{\varphi}{\done{\R}}}~\sigma \\
    \land~& ~\forall x, \sigma'.~ \R~x~\sigma' \to \wpx{u[x]}{\AU{\varphi}{\varphi'}}~\sigma'
    \end{align*}
\end{frame}

\begin{frame}{wp: Conditional}
    \begin{align*}
    &\wpx{\rif{c}{t}{u}}{\AU{\varphi}{\varphi'}} ~=~ \lambda~\sigma. \\
    & \quad \wpx{c}{\AU{\varphi}{\done{\R}}}~\sigma ~\land~ \forall~b,\sigma'.~ \R~b~\sigma' \to \\
    & \quad \begin{cases}
      \wpx{t}{\AU{\varphi}{\varphi'}}~\sigma', &\text{if } b \\
      \wpx{u}{\AU{\varphi}{\varphi'}}~\sigma', &\text{otherwise}
    \end{cases}
    \end{align*}
\end{frame}

\begin{frame}{wp: While Loop (AU)}
    \begin{align*}
    &\wpx{\rwhile{c}{t}}{\AU{\varphi}{\varphi'}} ~=~ \lambda~\sigma. \\
    & \quad \R~\sigma ~\land~ \forall~\sigma,~\R~\sigma \to \\
    & \quad \wpx{c}{\AU{\varphi}{\done{\R'}}}~\sigma ~\land~ \forall~b,\sigma'.~ \R'~b~\sigma' \to \\
    & \quad \begin{cases}
      \wpx{t}{\AU{\varphi}{\varphi'}}~\sigma' ~\lor \\
      \quad \wpx{t}{\AU{\varphi}{\done{(\lambda~\_~\sigma''.~
          \R~\sigma'' \land f~\sigma'' < f~\sigma)}}}~\sigma', &\text{if } b \\
      \varphi'(\sigma'), &\text{o.w.}
    \end{cases}
    \end{align*}
\end{frame}

\begin{frame}{wp: While Loop (AG)}
    \begin{align*}
    &\wpx{\rwhile{c}{t}}{\AG{\varphi}} ~=~ \lambda~\sigma. \\
    & \quad \R~\sigma ~\land~ \forall~\sigma,~\R~\sigma \to \\
    & \quad \wpx{\rwhile{c}{t}}{\varphi}~\sigma ~\land \\
    & \quad \wpx{c}{\AU{\varphi}{\doneq{(\tm{true},\sigma)}{}}}~\sigma ~\land \\
    & \quad \wpx{t}{\varphi~\AU{}~\done{(\lambda~\sigma'.~ \R~\sigma')}}~\sigma
    \end{align*}
    Coinductive: $\wpx{\rwhile{c}{t}}{\varphi}~\sigma$ is the coinductive hypothesis.
\end{frame}

\begin{frame}{wp: Function Call -- $f$ ensures $\AF{\done{\R_f}}$}
  \begin{block}{$f$ has terminating postcondition}
    $$\wpx{\rcall{f}{e_1, \ldots, e_i}}{\AU{\varphi}{\varphi'}} ~=~ \lambda~\sigma.~ \forall~x,\sigma'.~ \R_f~x~\sigma' \to \wpx{k~x}{\AU{\varphi}{\varphi'}}~\sigma'$$
    where $k$ is the continuation after the call.
  \end{block}

  \begin{block}{$f$ has temporal ensures $\psi$: transparent checking}
    Additionally assert $\psi \implies \varphi$ where $\varphi$ is the caller's obligation.
  \end{block}

  \begin{block}{$f$ ensures $\AG{\psi}$: cannot prove $\AF{}$ for it}
    If $f$ has $\AG{\psi}$ (diverges), then $\wpx{\rcall{f}{\vec{e}}}{\AF{\varphi'}}$ is \textbf{unprovable} --
    a diverging call can never reach the $\AF{}$ goal.
  \end{block}
\end{frame}

% ============================================================================
\section{Multi-Process Configuration}
% ============================================================================

\begin{frame}{Configuration}
  A \textbf{configuration} $C = \Config{P}{\sigma}{i}$:
  \vspace{-0.2cm}
  \begin{align*}
    P &: \PID \to \text{Term} && \text{process dictionary} \\
    \sigma &\in \text{State} && \text{shared mutable state} \\
    i &\in \PID && \text{active process}
  \end{align*}
  \vspace{-0.4cm}
  \begin{itemize}
    \item Temporal formulas $\varphi$ are over shared state $\sigma$
    \item $\WPx{C}{\varphi}$ -- multi-process wp, defined by cases on $P(i)$
  \end{itemize}
\end{frame}

\begin{frame}{Cooperative Scheduling}
  Rust \texttt{async} uses \textbf{cooperative scheduling}:
  each process runs uninterrupted until it reaches an \texttt{.await},
  then yields control to the scheduler.

  \vspace{0.3cm}
  \begin{itemize}
    \item \textbf{Within a process}: execution is sequential (single-process $\mathtt{wp}$ applies)
    \item \textbf{At \texttt{.await}}: the active process $i$ suspends; the scheduler
          picks a ready process $p$ to run (nondeterministic choice)
    \item \textbf{No preemption}: a process cannot be interrupted mid-statement;
          shared state $\sigma$ is only modified by the active process
  \end{itemize}

  \vspace{0.3cm}
  This means nondeterminism arises \emph{only} at \texttt{.await} boundaries ---
  between yield points, the program is deterministic and the single-process
  rules apply unchanged.
\end{frame}

% ============================================================================
\section{Multi-Process Weakest Preconditions}
% ============================================================================

\begin{frame}{WP: Sequential Step}
  When $P(i)$ is a non-async construct (assignment, let, if, while, sync call):

  $$\WPx{\Config{P}{\sigma}{i}}{\varphi} ~=~ \wpx{P(i)}{\varphi}~\sigma$$

  Delegate to single-process $\mathtt{wp}$. Process dictionary $P$ is unchanged.

  \vspace{0.5cm}
  Programs without \texttt{async}/\texttt{await} reduce entirely to single-process wp:
  $$\WPx{\Config{\{0 \mapsto t\}}{\sigma}{0}}{\varphi} ~=~ \wpx{t}{\varphi}~\sigma$$
\end{frame}

\begin{frame}{WP: Async (Spawn a Future)}
  \begin{block}{Rust: \quad $\rasync{e}$ evaluates to a fresh $p \in \PID$}
  \end{block}

  $$\WPx{\Config{P}{\sigma}{i}}{\varphi} ~=~ \WPx{\Config{P[p \mapsto e]}{\sigma}{i}}{\varphi}$$

  \begin{itemize}
    \item Adds new process $p$ with body $e$ to $P$ (suspended)
    \item Process $i$ continues running (active, unchanged)
    \item No state change, no scheduling --- futures are lazy
  \end{itemize}
\end{frame}

\begin{frame}{WP: Await (AU -- terminating callee)}
  \begin{block}{Rust: \quad $P(i) = \rlet{x}{\rawait{p}}{k~x}$}
  \end{block}

  \begin{align*}
  &\WPx{\Config{P}{\sigma}{i}}{\AU{\varphi}{\varphi'}} ~= \\
  & \quad \WPx{\Config{P}{\sigma}{p}}{\AU{\varphi}{\done{\R}}} \\
  & \quad \land~ \forall~x, \sigma'.~ \R~x~\sigma' \to \WPx{\Config{P[i \mapsto k[x]]}{\sigma'}{i}}{\AU{\varphi}{\varphi'}}
  \end{align*}

  \begin{itemize}
    \item Switch active process to $p$; run it maintaining $\varphi$
    \item After $p$ terminates with result $x$ and state $\sigma'$, resume $i$
  \end{itemize}
\end{frame}

\begin{frame}{WP: Await (AG -- diverging callee)}
  \begin{block}{Rust: \quad $P(i) = \rawait{p}$}
  \end{block}

  $$\WPx{\Config{P}{\sigma}{i}}{\AG{\varphi}} ~=~ \wpx{P(p)}{\AG{\varphi}}~\sigma$$

  \begin{itemize}
    \item Reduces to single-process $\mathtt{wp}$ on callee's body $P(p)$
    \item Process $p$ must satisfy $\AG{\varphi}$ (runs forever)
    \item Continuation after await is unreachable
  \end{itemize}
\end{frame}

% ============================================================================
\section{Vera Implementation}
% ============================================================================

\begin{frame}[fragile]{Vera Syntax}
  Vera extends Verus with temporal postconditions and loop annotations:

  \vspace{0.2cm}
  \begin{tabular}{l l l}
    \textbf{Annotation} & \textbf{TICL} & \textbf{Proof obligation} \\
    \hline
    \verb|ensures af(done(Q))| & $\AF{\done{Q}}$ & Loop + \verb|decreases| \\
    \verb|ensures ag(P)| & $\AG{\varphi}$ & Infinite loop + \verb|invariant| \\
    \verb|ensures au(P, done(Q))| & $\AU{P}{\done{Q}}$ & Loop + \verb|invariant| + \verb|decreases| \\
    \verb|ensures ag(af(now(Q)))| & $\AG{\AF{\now{Q}}}$ & Ghost accumulator + \verb|decreases| \\
    \hline
    \verb|invariant R| & $\R$ & Temporal refinement mapping \\
    \verb|decreases f| & $f$ & Well-founded metric (progress) \\
  \end{tabular}

  \vspace{0.3cm}
  \verb|done(Q)|: $Q$ holds at termination. \quad
  \verb|now(Q)|: $Q$ holds at some intermediate state.
\end{frame}

\begin{frame}[fragile]{Example: Queue Drain (AF)}
  \begin{block}{Liveness: the queue eventually becomes empty}
  \end{block}
  \vspace{-0.2cm}
  \begin{lstlisting}
fn drain(q: &mut Queue)
    requires q.view().len() > 0,
    ensures  af(done(q.view().len() == 0)),
{
    while q.len() > 0
        invariant q.view().len() >= 0,
        decreases q.view().len(),
    {
        let _ = q.dequeue();
    }
}
  \end{lstlisting}
  \vspace{-0.2cm}
  \verb|invariant| $= \R$, \quad
  \verb|decreases| $= f$ \quad $\Rightarrow$ \quad WhileAU$_L$ rule applies.
\end{frame}

\begin{frame}[fragile]{Example: Bounded Counter (AG)}
  \begin{block}{Safety: the counter stays bounded forever}
  \end{block}
  \vspace{-0.2cm}
  \begin{lstlisting}
fn bounded_counter(x: &mut u64)
    requires *x == 0,
    ensures  ag(*x <= 15),
{
    loop
        invariant *x <= 10,
    {
        if *x < 10 { *x = *x + 1; }
        else       { *x = 0; }
    }
}
  \end{lstlisting}
  \vspace{-0.2cm}
  No \verb|decreases| $\Rightarrow$ infinite loop $\Rightarrow$ WhileAG rule. \\
  \verb|invariant| $(\ast x \leq 10) \implies$ postcondition $(\ast x \leq 15)$.
\end{frame}

\begin{frame}[fragile]{Example: Bind Rule (AG + AF callee)}
  \begin{block}{AG caller invokes AF callee --- bind rule extracts postcondition}
  \end{block}
  \vspace{-0.2cm}
  \begin{lstlisting}
fn reset_to_zero(x: &mut u64)
    ensures af(done(*x == 0)),
{ *x = 0; }

fn ag_caller(x: &mut u64)
    requires *x == 0,
    ensures  ag(*x <= 10),
{
    loop
        invariant *x <= 10,
    {
        if *x < 10 { *x = *x + 1; }
        if *x > 5  { reset_to_zero(x); }
    }
}
  \end{lstlisting}
  \vspace{-0.2cm}
  After \verb|reset_to_zero|: assume $\ast x = 0$ (bind rule $\R$). Invariant preserved.
\end{frame}

\begin{frame}[fragile]{Example: Round-Robin Fairness (AG(AF(now)))}
  \begin{block}{Every element eventually reaches the head of the queue}
  \end{block}
  \vspace{-0.2cm}
  \begin{lstlisting}
fn round_robin(queue: &mut Queue, Ghost(x): Ghost<u64>)
    requires
        queue.view().len() > 1,
        queue.view().contains(x),
    ensures
        ag(queue.view().len() > 0),
        ag(af(now(queue.peek_spec() == x))),
{
    loop
        invariant
            queue.view().len() > 1,
            queue.view().contains(x),
        decreases queue.view().index_of_first(x),
    {
        let val = queue.dequeue();
        queue.enqueue(val);
    }
}
  \end{lstlisting}
  \vspace{-0.3cm}
  {\footnotesize \verb|now()| = state predicate (head $= x$ at loop start, not end). Ghost accumulator tracks it.}
\end{frame}

% ============================================================================
\section{Summary}
% ============================================================================

\begin{frame}{Complete Rule Set}
  \begin{center}
  \small
  \begin{tabular}{l l l}
    \textbf{Construct} & \textbf{Rule} & \textbf{Layer} \\
    \hline
    $\rmut{x}{e}$ & State update & $\mathtt{wp}$ \\
    $\rlet{x}{t}{u}$ & BindAU$_L$ & $\mathtt{wp}$ \\
    $\rif{c}{t}{u}$ & Case split (effectful $c$) & $\mathtt{wp}$ \\
    $\rwhile{c}{t}$ + \texttt{dec.} & WhileAU$_L$ & $\mathtt{wp}$ \\
    $\rwhile{c}{t}$ (no exit) & WhileAG & $\mathtt{wp}$ \\
    $\rcall{f}{\vec{e}}$ & Contract-dependent & $\mathtt{wp}$ \\
    \hline
    Sequential step & Delegate to $\mathtt{wp}$ & $\mathtt{WP}$ \\
    $\rasync{e}$ & Extend $P$, no-op & $\mathtt{WP}$ \\
    $\rawait{p}$ (AU callee) & Switch active to $p$ & $\mathtt{WP}$ \\
    $\rawait{p}$ (AG callee) & Divergence & $\mathtt{WP}$ \\
  \end{tabular}
  \end{center}

  \vspace{0.3cm}
  Two layers: $\wpx{t}{\varphi}$ for sequential Rust,
  $\WPx{\Config{P}{\sigma}{i}}{\varphi}$ for async concurrency.
\end{frame}

\end{document}
